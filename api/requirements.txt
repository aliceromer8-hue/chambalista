# Dependencias del despliegue en Vercel.
#
# Es una copia de requirements-web.txt a propósito: Vercel lee el
# requirements.txt que está junto a la función, y el de la raíz incluye
# Playwright, que aquí no sirve —no hay navegador de nadie que usar— y
# pesa cientos de megas.
flask>=3.0
python-docx>=1.1
pdfplumber>=0.11
truststore>=0.9
