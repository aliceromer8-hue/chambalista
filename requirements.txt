# Dependencias de la versión WEB (app_web.py). Es el requirements que
# leen Vercel y cualquier otro alojamiento por convención, así que aquí
# va lo que se despliega, no lo que se usa en local.
#
# Playwright NO está aquí a propósito: en un servidor no hay ninguna
# sesión de navegador que usar, y pesa cientos de megas. Para la versión
# local que sí postula, ver requirements-local.txt.
flask>=3.0
python-docx>=1.1
pdfplumber>=0.11
truststore>=0.9
