# Dependencias de la versión web pública (app_web.py).
# NO incluye Playwright: la web no abre navegadores ni postula, así que
# el despliegue queda pequeño y arranca rápido en la capa gratuita.
flask>=3.0
gunicorn>=22.0
python-docx>=1.1
pdfplumber>=0.11
truststore>=0.9
